DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=university_db

# Secret key for signing JWTs
JWT_SECRET=yourSecretKey

# JWT token expiry (e.g. 1h, 24h)
TOKEN_EXPIRY=1h

# Application port
PORT=5000